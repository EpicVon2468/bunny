VERSION 0 0 0

funct main(arg_count: i32, args: **u8): i32 {
	define a: i32 = 2;
	define mutable b: i32 = 2;
	b = a + b;
	return b;
}

funct abc(...) {
}

funct def();

type ghi {
	define jkl: i32;
}

// use empty brackets instead of semicolon to avoid confusion between empty structs and undefined structs
type mno {}